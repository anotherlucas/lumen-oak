import styled from 'styled-components';

export const Flexconomy = styled.div`
  position: relative;
  display: flex;
  flex-direction: column;
  justify-content: flex-start;
  width:58rem;
  max-width:calc(100% - 4rem);
  margin: -1rem auto 0;
`;

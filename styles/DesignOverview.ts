import styled from 'styled-components';

export const DesignOverview = styled.div`
  position: relative;
  display: flex;
  flex-direction: column;
  justify-content: flex-start;
  width:58rem;
  max-width:calc(100% - 4rem);
  margin: 0 auto 3rem;
`;